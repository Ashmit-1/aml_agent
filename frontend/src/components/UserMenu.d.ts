interface UserMenuProps {
    /** Position the dropdown to the left instead of right */
    align?: 'left' | 'right';
}
export declare const UserMenu: ({ align }: UserMenuProps) => import("react").JSX.Element | null;
export {};
