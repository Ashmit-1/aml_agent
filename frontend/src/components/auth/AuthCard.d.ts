interface AuthCardProps {
    title: string;
    children: React.ReactNode;
    footer?: React.ReactNode;
}
export declare const AuthCard: ({ title, children, footer }: AuthCardProps) => import("react").JSX.Element;
export {};
